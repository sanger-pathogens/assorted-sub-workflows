params {
    // Step 02 - Colour mapping (color_mapping.nf)
    assembly_input  = "" // a directory of assemblies, or a txt file listing assembly paths (one per line)
    assembly_suffix = ""
    metadata        = ""
    sample_col      = ""
    label_col       = ""

    // Shared across GGCAT / SBWT / Themisto2 builds
    kmer_size = 31
    ggcat_temp_dir = ""

    // Step 04 - SBWT
    sbwt_temp_dir = ""

    // Step 05-06 - Themisto2
    gzip_export = false
}

// finish off the remaining configs to define
